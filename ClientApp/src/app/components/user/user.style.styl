@import '../../../common/colors.styl'

.user
    .icon
        color white
        transition 0.2s ease all
        &.active
            color color-yellow-rum-swizzle
    &:hover .icon:hover
        color color-yellow-rum-swizzle