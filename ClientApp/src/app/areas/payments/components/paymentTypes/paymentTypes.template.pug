doctype html

.types-list
    .types-list__item(
        *ngFor='let type of (paymentTypes$ | async)'
    )
        .header
            span.oi.oi-trash.oi-medium(
                (click)='onDeleteClick(type)'
            )
            h5 {{ type.name }}
            
        p {{ type.description}}