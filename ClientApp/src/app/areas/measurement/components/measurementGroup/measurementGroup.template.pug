doctype html

.card.m-card
    .card-header {{ formattedGroupName }}
    .card-body
        .measurement-header(
            [class.measurement-header--flagged]='(isSentFlagActive | async)'
        )
            .measurement-header__flag(
                *ngIf='(isSentFlagActive | async)'
            )
                span Sent
            .measurement-header__type
                span Type
            .measurement-header__value
                span Value
            .measurement-header__comment
                span Comment
            .measurement-header__actions
                span Actions
        app-measurement-item(
            *ngFor='let measurement of measurementGroup.measurements',
            [measurement]='measurement',
            [isSentFlagActive]='isSentFlagActive',
            [onDeleteClick]='onDeleteClick$',
            [onEditClick]='onEditClick$',
            [onTypeClick]='onTypeClick$',
            [onSendFlagClick]='onSendFlagClick$',
        )